# 猫超补货 RPA/Web 系统交接文档

更新时间：2026-08-13

本文用于把当前猫超补货 RPA 项目的业务范围、代码结构、部署方式、账号安全、日常使用、排错方式和已知风险完整交接给后续开发、运维、前端会话或 mentor。文档不包含任何账号密码、SSH 密码或密钥内容。

## 1. 当前结论

系统已经从单机脚本推进到“后端 API + Web 管理台 + RPA Worker + 加密账号库”的形态。

运营同事访问 Web 管理台创建任务、查看排队/运行/成功/失败、下载文件、查看错误截图。后端和 Worker 目前部署在一台长期不关机的 Windows 主机上，浏览器由 Worker 在这台机器上控制。

当前对外访问地址：

```text
http://172.17.10.33:8000
```

当前 Windows 主机信息：

```text
IP: 172.17.10.33
Hostname: DESKTOP-50RVR77
Windows 项目目录: C:\Users\devuser.DESKTOP-50RVR77\maochao_rpa
```

本地开发目录：

```text
/Users/laya/Desktop/运营数据分析/补货工作流/maochao_rpa
```

GitHub 仓库：

```text
git@github.com:Layayaa/maochao-rpa.git
```

## 2. 业务范围

本系统只覆盖猫超补货表数据源下载中以下 6 个任务。Web 前端为了运营同事理解，把任务显示为 1-6；RPA 内部仍保留真实需求编号。

| Web 显示 | 真实需求编号 | task_key | 任务名称 | 说明 |
|---:|---:|---|---|---|
| 任务 1 | 1 | `realtime-inventory` | 实时库存 | 每个供应商都要尝试导出；有库存则下载，无库存则记录无数据 |
| 任务 2 | 2 | `pincang-detail` | 库存分析-品仓明细表 | 导出后归档和清洗 |
| 任务 3 | 3 | `system-order` | 系统单 | 默认状态包含“待供应商确认”“PB审批:库控小二” |
| 任务 4 | 4 | `po-list` | 补货单列表 | 采购补货单列表导出 |
| 任务 5 | 10 | `channel-goods` | 库位明细 | 走“商品 -> 渠道货品 -> 筛选 -> 导出” |
| 任务 6 | 11 | `transfer-order` | 调拨单 | 导出货品明细；清洗时过滤已配置的完成状态 |

明确不做：

| 真实需求编号 | 名称 |
|---:|---|
| 5 | E3库存 |
| 6 | 库存锁定流水账 |
| 7 | 库存锁定单 |
| 8 | 出库流水账 |
| 9 | WMS数据 |

如果后续有人看到“任务 5/任务 6”，要先确认说的是 Web 前端显示编号，还是原需求编号。当前 Web 的“任务 5”实际是原需求 `10、库位明细`；Web 的“任务 6”实际是原需求 `11、调拨单`。

## 3. 系统架构

```text
运营同事浏览器
  |
  | 访问 http://172.17.10.33:8000
  v
FastAPI 后端 api_server.py
  |
  | 读写 backend/rpa.db 队列、运行记录、锁
  | 读账号库 accounts/maochao_accounts.db
  | 托管 web/index.html, web/app.js, web/styles.css
  v
RPA Worker rpa_worker.py
  |
  | 每 2 秒轮询 pending run
  | 领取后加 account_locks
  v
maochao_rpa.py + Playwright + Chrome
  |
  | 一个账号一个 Chrome profile
  | 一个账号一个 remote debugging port
  v
猫超网页
  |
  | 下载文件
  v
downloads/ 原始下载 -> data/ 归档清洗 -> Web 下载中心
```

核心文件：

| 文件/目录 | 作用 |
|---|---|
| `api_server.py` | FastAPI 后端，提供接口并托管 Web 页面 |
| `rpa_worker.py` | Worker 进程，轮询队列并执行 RPA |
| `backend_core.py` | SQLite 队列、运行记录、账号锁、Worker 心跳 |
| `maochao_rpa.py` | Playwright RPA 主逻辑 |
| `account_store.py` | 加密账号数据库，密码不明文返回给前端 |
| `web/` | Web 管理台静态前端 |
| `scripts/windows/` | Windows 启动、状态、停止、自启脚本 |
| `config.example.json` | 示例配置，不放真实账号 |
| `config.local.json` | 真实本地配置，不提交 Git |
| `accounts/` | 加密账号库和密钥，不提交 Git |
| `backend/` | 后端 SQLite 队列库、Worker 心跳，不提交 Git |
| `browser_profiles/` | Chrome 登录态，不提交 Git |
| `downloads/` | 浏览器原始下载目录，不提交 Git |
| `data/` | 归档和清洗后的输出文件，不提交 Git |
| `logs/` | 后端、Worker、RPA 日志和失败截图，不提交 Git |

## 4. 当前部署现状

Windows 主机部署目录：

```bat
C:\Users\devuser.DESKTOP-50RVR77\maochao_rpa
```

Windows 已有内容：

```text
Python 虚拟环境: .venv
后端入口: api_server.py
Worker 入口: rpa_worker.py
配置: config.local.json
账号库: accounts\maochao_accounts.db
账号库密钥: accounts\.secret_key
Chrome: C:\Program Files\Google\Chrome\Application\chrome.exe
```

`config.local.json` 在 Windows 上使用加密账号库：

```json
{
  "accounts_source": "db",
  "accounts_db_path": "./accounts/maochao_accounts.db",
  "accounts_db_key_path": "./accounts/.secret_key"
}
```

当前健康检查曾确认：

```text
API: online
Worker: online
pending_count: 0
running_count: 0
```

注意：Windows 上曾遇到 SSH 会话无法终止旧 API/Worker 进程的问题，原因是 `Get-CimInstance` 和 `taskkill /F` 返回“拒绝访问”。因此如果要彻底替换正在运行的进程，最稳妥方式是在 Windows 机器本机登录会话中运行停止脚本，或者重启该用户会话/机器后再启动。仅通过 SSH 有时只能启动和探活，不能可靠杀掉旧进程。

## 5. 账号、浏览器、端口规则

这是系统最重要的约束。不要随意改变。

一个账号必须对应：

```text
1 个 account_key
1 个 Chrome profile_dir
1 个 remote debugging port
1 个 download_dir
```

当前账号示例：

| account_key | port | profile_dir | download_dir | 用途 |
|---|---:|---|---|---|
| `tmall_inventory_01` | `9221` | `browser_profiles/tmall_inventory_01` | `downloads/tmall_inventory_01` | 实时库存、库位明细等库存相关 |
| `tmall_common_01` | `9230` | `browser_profiles/tmall_common_01` | `downloads/tmall_common_01` | 品仓明细、系统单、补货单、调拨单等通用任务 |

安全规则：

- 不要使用无痕模式。无痕模式无法稳定保留登录态，会增加验证码和手机验证风险。
- 不要让两个账号共用同一个 `profile_dir`。
- 不要让两个账号共用同一个 `port`。
- 不要同时启动多个 Worker 去抢同一套账号、端口和浏览器 profile。
- 不要频繁重复登录同一账号，避免触发手机验证码、滑块或安全验证。
- 调试时优先复用已有登录态；只有确认登录态失效时再手动登录。

系统层面的保护：

- 账号库禁止重复 `port` 和重复 `profile_dir`。
- Worker 执行前会写入 `account_locks`。
- 同一个账号、端口、profile 被锁住时，第二个 run 不能同时使用它。
- Web 前端不会返回账号密码，只显示是否已设置用户名和密码。

## 6. XPath 和账号差异

猫超页面在不同账号下可能出现不同文案、不同供应商列表、不同数量的供应商、不同菜单权限。

配置里预留了两类账号级差异：

| 字段 | 适合放什么 |
|---|---|
| `xpath_vars` | 文本变量，例如供应商名、商家类型、二级供应商文案 |
| `selector_overrides` | 某个账号需要完全替换某个 selector/XPath 时使用 |

例子：

```json
{
  "xpath_vars": {
    "supplier_name": "某供应商",
    "merchant_type_text": "商品供应商"
  },
  "selector_overrides": {
    "realtime": {
      "supplier_first_option": "//li[contains(normalize-space(.), '{{supplier_name}}')]"
    }
  }
}
```

当前 RPA 对实时库存供应商做了兜底：

- 如果账号配置了 `supplier_names`，优先按配置逐个处理。
- 如果配置了 `__first__`，会选择页面里第一个可见供应商。
- 如果供应商文本在不同账号不一致，优先把差异放入账号配置，不要硬编码到全局 XPath。
- 有库存的供应商应导出文件；无库存的供应商应记录为 `ok` 但无下载文件，而不是硬失败。

## 7. 最近关键修复

最近失败主要发生在 `tmall_inventory_01` 登录后的“选择商家账号”页面。

失败现象：

```text
登录成功后停在“登录成功，选择商家账号”
页面提示缺少“商家类型”和“二级供应商”
最终报错：登录未完成：未进入商家主页，请检查验证码/滑块/登录态。
```

已修复：

- `maochao_rpa.py` 的 `_handle_merchant_selector()` 现在会先选择商家类型。
- 然后选择二级供应商。
- 最后点击“进入商家”。

新增/调整的辅助逻辑：

- `_select_merchant_type`
- `_select_second_supplier`
- `_click_first_dropdown_option`

Windows 上已同步过该修复，并通过 CLI 登录验证：

```bat
cd /d %USERPROFILE%\maochao_rpa
.venv\Scripts\python.exe maochao_rpa.py login --config config.local.json --account tmall_inventory_01 --headed
```

验证输出曾确认：

```text
商家类型已选择: 商品供应商
二级供应商已选择。
登录态可用，继续执行。
```

## 8. Web 管理台使用说明

运营同事只需要打开：

```text
http://172.17.10.33:8000
```

日常操作：

1. 先看顶部状态。
2. 如果显示 API 在线、Worker 在线，可以创建任务。
3. 选择账号和任务。
4. 点击运行。
5. 任务会进入队列。
6. 至少一个任务应显示“运行中”，其他任务可显示“排队中”。
7. 成功后到下载中心下载文件。
8. 失败后到错误中心查看错误、日志和截图。

队列功能：

- `pending` 排队中：可以取消、上移、下移。
- `running` 运行中：不能取消或调顺序；可以请求暂停，当前子任务完成后暂停。
- `paused` 已暂停：可以继续运行。
- `succeeded` 成功：可以下载文件。
- `failed` 失败：去错误中心看原因。

重要说明：

- “成功”不等于每个子任务都有文件。有些任务本来就可能无数据，RPA 会记录为 `ok`，但不会生成下载文件。
- 下载中心只展示实际生成到 `data/` 目录的文件。
- 如果 run 成功但下载中心没有某个任务文件，要先看该 run 的结果备注是否是“无数据/文件任务返回 null/未生成下载文件”。

## 9. 后端和 Worker 启停

在 Windows 本机命令行中：

```bat
cd /d %USERPROFILE%\maochao_rpa
scripts\start_maochao.cmd
```

查看状态：

```bat
cd /d %USERPROFILE%\maochao_rpa
scripts\status_maochao.cmd
```

停止：

```bat
cd /d %USERPROFILE%\maochao_rpa
scripts\stop_maochao.cmd
```

已配置开机/登录自启脚本：

```text
%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\maochao_rpa_start.cmd
```

启动脚本逻辑：

- 自动定位项目根目录。
- 检查 `.venv\Scripts\python.exe` 是否存在。
- 创建 `logs`、`data`、`downloads`、`browser_profiles`、`backend` 目录。
- 如果 API 已在线，不重复启动 API。
- 如果 Worker 已在线，不重复启动 Worker。
- 分别启动 `api_server.py` 和 `rpa_worker.py`。
- 标准输出和错误日志写入 `logs/`。

状态脚本会访问：

```text
http://127.0.0.1:8000/api/health
http://127.0.0.1:8000/api/worker
```

## 10. 常用接口

给开发和排错使用：

```bash
curl -sS --max-time 10 http://172.17.10.33:8000/api/health
curl -sS --max-time 10 http://172.17.10.33:8000/api/worker
curl -sS --max-time 10 http://172.17.10.33:8000/api/tasks
curl -sS --max-time 10 http://172.17.10.33:8000/api/accounts
curl -sS --max-time 10 http://172.17.10.33:8000/api/runs
curl -sS --max-time 10 http://172.17.10.33:8000/api/errors
curl -sS --max-time 10 http://172.17.10.33:8000/api/files
```

接口含义：

| 接口 | 用途 |
|---|---|
| `/api/health` | API 进程、配置、账号库路径、任务数量 |
| `/api/ready` | 配置、账号库、目录是否齐全 |
| `/api/worker` | Worker 是否在线、当前 running run、pending 数量 |
| `/api/tasks` | 后端任务列表 |
| `/api/accounts` | 账号列表，不含明文密码 |
| `/api/runs` | run 队列和历史 |
| `/api/runs/{run_id}` | 单个 run 详情 |
| `/api/runs/{run_id}/logs` | 单个 run 日志 |
| `/api/runs/{run_id}/errors` | 单个 run 错误 |
| `/api/runs/{run_id}/files/download` | 打包下载该 run 的输出文件 |
| `/api/errors` | 全部错误中心 |
| `/api/files` | 下载中心文件列表 |
| `/api/screenshots/{screenshot_id}` | 查看失败截图 |

Worker 在线判断：

- Worker 每 2 秒写 `backend/worker_heartbeat.json`。
- 超过 8 秒未更新，API 会认为 `worker_online=false`。
- API 在线不代表 Worker 在线。

## 11. 账号维护

账号保存在加密 SQLite 数据库里：

```text
accounts/maochao_accounts.db
accounts/.secret_key
```

账号导入方式：

```bash
python account_store.py import-json config.local.json
```

如果本机没有 `python` 命令，用明确的 Python 路径或 `python3`。在 Windows 虚拟环境里：

```bat
cd /d %USERPROFILE%\maochao_rpa
.venv\Scripts\python.exe account_store.py import-json config.local.json
```

注意：

- 导入前的 JSON 可以临时含账号密码。
- 导入完成后，不要把含明文密码的 JSON 提交到 Git。
- 临时导入文件用完要删除。
- `accounts/` 目录不要提交，不要发给无权限的人。
- Web 端更新账号时，后端写入加密账号库；正在运行中的 run 不会中途切换配置。

## 12. 文件输出规则

运行时涉及三层文件：

| 目录 | 内容 |
|---|---|
| `downloads/<account_key>/` | Chrome 原始下载落点 |
| `data/raw/...` 或等价归档路径 | 归档后的原始文件 |
| `data/cleaned/...` 或等价清洗路径 | 清洗后的可用文件 |

RPA 结果里通常会记录：

```text
raw_file
cleaned_file
note
```

下载中心扫描 `data_root`，不是扫描浏览器默认“下载”目录。用户如果在 Windows 的“下载”文件夹里找不到文件，不代表 RPA 没产出；应先打开 Web 下载中心或检查项目目录下的 `data/`。

任务可能成功但没有文件的情况：

- 实时库存某个供应商无库存。
- 补货单列表无数据。
- 调拨单无数据。
- 猫超文件任务返回 null 或无下载文件，RPA 判断为可跳过。

这类情况应显示为任务结果 `ok` 加备注，而不是系统失败。

## 13. 日志和截图

日志目录：

```text
logs/
```

失败截图目录：

```text
logs/screenshots/
```

RPA 子任务失败时会自动截图，文件名通常包含：

```text
task_key
account_key
failed
时间
```

排错优先级：

1. Web 错误中心。
2. 单个 run 的日志。
3. 单个 run 的错误详情。
4. `logs/screenshots/` 里的失败截图。
5. Windows 上 API/Worker 的 stdout/stderr 日志。

Windows 常见日志文件：

```text
logs\api_server.out.log
logs\api_server.err.log
logs\rpa_worker.out.log
logs\rpa_worker.err.log
logs\<run_id>.log
```

## 14. 常见问题处理

### Web 显示 “RPA Worker 当前离线”

含义：

- API 后端还活着。
- Worker 心跳没有更新。
- 新任务可以进入队列，但不会开始执行。

处理：

```bat
cd /d %USERPROFILE%\maochao_rpa
scripts\start_maochao.cmd
scripts\status_maochao.cmd
```

如果 status 仍显示离线：

- 确认 Windows 主机没有休眠。
- 确认 `.venv` 存在。
- 查看 `logs\rpa_worker.err.log`。
- 确认 `config.local.json`、`accounts/maochao_accounts.db`、`accounts/.secret_key` 存在。

### Web 一直显示排队中

先查：

```text
/api/worker
```

判断：

- `worker_online=false`：启动 Worker。
- `worker_online=true` 但 `running_run_id` 为空且 `pending_count>0`：看 Worker 日志，可能 Worker 领取任务时报错。
- 有任务正在 `running`：后面的任务排队是正常的，因为当前 Worker 是单进程串行。

### 点运行后直接跑了任务 6

要确认前端选中的任务集合、后端收到的 `task_keys`、账号允许的 `tasks`。

当前创建 run 时前端会传：

```json
{
  "force_account_tasks": true
}
```

这意味着前端选择的任务会强制按选择执行，不只按账号默认任务过滤。若看到顺序异常，优先查：

- `/api/runs/{run_id}` 里的 `task_keys`
- `web/app.js` 的 `TASK_ORDER`
- `maochao_rpa.py` 的 `TASKS`
- 当前账号配置里的 `tasks`

### 成功了但下载中心没有文件

先看 run 详情里的 `result`。

如果某个 result 的 `note` 写着无数据、null、未生成下载文件，这是业务无数据，不是下载失败。

如果 result 有 `raw_file` 或 `cleaned_file`，但下载中心没有：

- 检查路径是否在 `data_root` 下。
- 检查文件是否真实存在。
- 检查 `/api/files` 是否能扫描到。

### 登录要求手机验证码/滑块

不要让 RPA 继续反复尝试。

处理：

1. 暂停或停止相关任务。
2. 在 Windows 主机上用对应账号的 headed 浏览器手动完成验证。
3. 确认进入猫超主页。
4. 再运行一次登录验证。

示例：

```bat
cd /d %USERPROFILE%\maochao_rpa
.venv\Scripts\python.exe maochao_rpa.py login --config config.local.json --account tmall_inventory_01 --headed
```

### 又跳到不相关页面

例如曾经误跳到“寄售进销存台账”。当前需求没有该任务。

处理方向：

- 不要为了适配错误页面新增错误业务流程。
- 回看当前任务的菜单 XPath 或 direct_url。
- 确认配置里没有错误直达 URL。
- 失败截图中确认当前页面面包屑/菜单。
- 只修对应任务的打开路径。

## 15. 开发环境

本地开发目录：

```bash
cd "/Users/laya/Desktop/运营数据分析/补货工作流/maochao_rpa"
```

安装依赖：

```bash
python3 -m pip install -r requirements.txt
```

语法检查：

```bash
python3 -m py_compile maochao_rpa.py api_server.py backend_core.py rpa_worker.py account_store.py
```

后端自测：

```bash
python3 backend_selftest.py
```

本地启动 API：

```bash
python3 -u api_server.py
```

本地启动 Worker：

```bash
python3 -u rpa_worker.py
```

本地 dry-run：

```bash
python3 maochao_rpa.py dry-run --config config.local.json
```

本地手动登录验证：

```bash
python3 maochao_rpa.py login --config config.local.json --account tmall_inventory_01 --headed
```

本地单任务调试：

```bash
python3 maochao_rpa.py run --config config.local.json --account tmall_inventory_01 --task 1 --headed
```

## 16. 部署更新流程

推荐流程：

1. 本地改代码。
2. 本地跑语法检查和必要自测。
3. 提交 Git。
4. 推送 GitHub。
5. 同步到 Windows 主机。
6. 重启 API/Worker 或确认新进程加载了新代码。
7. 做一次小范围任务验证。
8. 再交给运营使用。

如果 Windows 没有 Git 或 Git 不在 PATH，可用 `scp` 同步文件或压缩包。曾经使用过：

```bash
scp maochao_rpa.py devuser@172.17.10.33:maochao_rpa/maochao_rpa.py
scp scripts/windows/* devuser@172.17.10.33:maochao_rpa/scripts/
```

如果改了多个文件，不建议只同步单个文件；要同步完整变更集合，避免 API、Worker、RPA 版本不一致。

特别注意：

- Windows 旧进程如果没重启，可能还在运行旧代码。
- 通过 SSH 有时无法 kill 旧进程。
- 如果确认必须加载新代码，最好在 Windows 本机用户会话里执行停止和启动，或重启机器。

## 17. Git 和敏感文件

不要提交：

```text
config.local.json
accounts/
backend/
browser_profiles/
downloads/
data/
logs/
*.db
*.sqlite
*.secret*
含账号密码的临时 JSON
```

可以提交：

```text
api_server.py
rpa_worker.py
backend_core.py
maochao_rpa.py
account_store.py
web/
scripts/windows/
README.md
API_BACKEND.md
config.example.json
交接文档.md
requirements.txt
backend_selftest.py
```

当前本地 Git 状态曾显示：

```text
M maochao_rpa.py
?? scripts/
```

说明：

- `maochao_rpa.py` 有未提交修改，主要和商家选择修复有关。
- `scripts/` 是新增 Windows 启停脚本目录。
- 提交前必须确认没有把真实账号、密码、数据库、密钥、下载文件放进 Git。

## 18. 运营 SOP

给运营同事的简版流程：

1. 打开 `http://172.17.10.33:8000`。
2. 确认顶部没有 Worker 离线提示。
3. 选择要跑的任务和账号。
4. 点击运行。
5. 运行中不要重复点很多次。
6. 需要调整未开始任务时，在队列里上移、下移或取消。
7. 成功后去下载中心取文件。
8. 如果失败，截图或复制错误中心信息给维护人员。
9. 如果页面提示验证码、滑块、手机验证，暂停，不要反复重试。

运营同事不需要：

- 进 Windows 项目目录。
- 改账号库。
- 改 XPath。
- 手动看后端日志。
- 手动打开 Chrome profile。

## 19. 维护人员 SOP

接到报错后，按这个顺序：

1. 查 `/api/worker`，确认 Worker 在线。
2. 查 `/api/runs/{run_id}`，看 run 状态、账号、任务、result。
3. 查 `/api/runs/{run_id}/errors`。
4. 查 `/api/runs/{run_id}/logs`。
5. 打开失败截图。
6. 判断是登录态、验证码、页面 XPath、无数据、下载中心找不到文件，还是 Worker/API 问题。
7. 如果是账号差异，优先改账号级 `xpath_vars` 或 `selector_overrides`。
8. 如果是全局页面结构变化，再改 `config.example.json` 的 selector 或 `maochao_rpa.py` 的通用逻辑。
9. 修完后先跑单账号单任务。
10. 确认文件产出和 Web 展示正常后，再跑全量。

不要一上来全量重跑。全量重跑会增加验证码风险，也会让日志更难定位。

## 20. 应急恢复

### 情况 A：API 离线，网页打不开

在 Windows 本机执行：

```bat
cd /d %USERPROFILE%\maochao_rpa
scripts\start_maochao.cmd
scripts\status_maochao.cmd
```

如果仍打不开：

- 确认主机 IP 没变。
- 确认防火墙没有拦截 8000。
- 查看 `logs\api_server.err.log`。
- 确认 8000 端口没有被其他程序占用。

### 情况 B：API 在线，Worker 离线

执行：

```bat
cd /d %USERPROFILE%\maochao_rpa
scripts\start_maochao.cmd
scripts\status_maochao.cmd
```

如果 Worker 仍离线，看：

```text
logs\rpa_worker.err.log
backend\worker_heartbeat.json
config.local.json
accounts\maochao_accounts.db
accounts\.secret_key
```

### 情况 C：队列卡住

先不要狂点运行。检查：

```text
/api/worker
/api/runs
logs\rpa_worker.err.log
```

如果有 `running` 任务但实际 Chrome 已关闭或卡死，需要在 Windows 本机确认并停止 Worker，再重新启动。通过 SSH 停止可能失败。

### 情况 D：账号登录态失效

在 Windows 本机用对应账号 headed 模式登录一次：

```bat
cd /d %USERPROFILE%\maochao_rpa
.venv\Scripts\python.exe maochao_rpa.py login --config config.local.json --account tmall_inventory_01 --headed
```

手动完成验证码/滑块后，不要换 profile，不要无痕。

### 情况 E：代码已同步但行为还是旧的

大概率是旧 Worker/API 进程没重启。

处理：

- 在 Windows 本机运行 `scripts\stop_maochao.cmd`。
- 如果停止脚本失败，重启 Windows 用户会话或机器。
- 再运行 `scripts\start_maochao.cmd`。
- 用 `/api/health` 看 API pid 是否变化。

## 21. 已知限制和风险

当前限制：

- Worker 是单进程串行执行，后续任务排队是正常行为。
- 暂停是协作式暂停，不会强杀正在下载或正在执行中的点击步骤。
- 多账号可以配置，但不要多 Worker 并发抢同一 profile/port。
- Windows SSH 会话可能无法杀掉旧进程。
- 猫超页面结构和 XPath 可能随账号、权限、供应商、页面更新变化。
- 某些任务无数据时不会产生文件。
- Web 下载中心只看 `data_root`，不看系统下载目录。

风险点：

- 账号验证码风险：频繁失败登录会触发手机验证。
- 登录态污染风险：共用 profile 或无痕会破坏稳定性。
- 版本不一致风险：只同步部分文件、没重启进程，会导致本地代码和 Windows 运行代码不一致。
- XPath 脆弱性：绝对 XPath 很容易被页面结构变动影响。
- 权限差异风险：不同账号看到的菜单、供应商、商家类型、二级供应商可能不同。

## 22. 建议后续优化

短期优先：

1. 给 Windows 运行进程加更可靠的服务管理方式，例如 NSSM 或任务计划程序。
2. Web 上更清楚地区分“成功且有文件”和“成功但无数据”。
3. 在 run 详情里直接展示每个子任务的截图入口、日志片段和文件路径。
4. 给账号配置编辑页加字段校验，避免重复端口/profile。
5. 给部署加版本号接口，避免不知道 Windows 当前跑的是哪版代码。

中期优化：

1. 把 XPath 调试结果沉淀到账号级配置，减少改代码次数。
2. 把 Chrome profile 和输出目录迁到固定数据盘路径。
3. 建立每日巡检：API、Worker、磁盘空间、最近一次成功运行。
4. 增加 run 结果导出报告，方便发给运营或 mentor。
5. 把部署流程改为 Git 拉取或标准 release zip，减少手工 scp 单文件。

长期优化：

1. 多 Worker 调度要做账号级资源隔离后再考虑。
2. 对高频任务做页面稳定性监控和 XPath 自动探测。
3. 给账号权限、供应商列表、任务可用性做启动前检查。
4. 增加权限分级：运营只跑任务和下载，维护人员才可改账号和 XPath。

## 23. 交接给新会话的提示词

如果要新开一个 Codex 会话继续维护，可以直接发：

```text
你接手猫超补货 RPA/Web 项目。请先阅读：
/Users/laya/Desktop/运营数据分析/补货工作流/maochao_rpa/交接文档.md
/Users/laya/Desktop/运营数据分析/补货工作流/maochao_rpa/README.md
/Users/laya/Desktop/运营数据分析/补货工作流/maochao_rpa/API_BACKEND.md

项目目录：
/Users/laya/Desktop/运营数据分析/补货工作流/maochao_rpa

GitHub：
git@github.com:Layayaa/maochao-rpa.git

Windows 运行地址：
http://172.17.10.33:8000

请不要读取、输出或提交任何账号密码、accounts/.secret_key、账号数据库、config.local.json、browser_profiles、downloads、data、logs。当前重点是保持运营 Web 可用、Worker 在线、账号登录态稳定，避免重复登录触发验证码。所有调试优先单账号单任务，失败时先看 Web 错误中心、run 日志和 logs/screenshots 截图。
```

## 24. 最后交接备注

这个项目当前已经能给运营同事展示 Web 管理台，也有后端队列、Worker 心跳、错误中心、下载中心和账号加密存储。最需要谨慎的不是代码本身，而是登录态和 Windows 运行进程管理。

后续任何人接手时，优先保证三件事：

1. 不泄露账号密码和密钥。
2. 不让多个 Worker 或多个浏览器 profile 抢同一账号。
3. 每次改动后确认 Windows 实际运行进程已经加载新代码。

## 25. 2026-08-13 最新业务口径（移交 Cursor）

本节是 2026 年 8 月 13 日与运营确认后的最新需求，优先级高于本文前面“任务 1 遍历实时库存页全部供应商”的旧描述。当前仅完成需求梳理，代码、配置、账号库均未按本节修改。

### 25.1 核心业务模型

猫超登录后进入的是一个公司的大账户。大账户右上角可以切换供应商，当前供应商是后续任务执行的业务上下文。

系统不是：

```text
登录一个账号 -> 任务 1 遍历页面里的所有供应商
```

系统应该是：

```text
登录一个猫超公司大账户
  -> 在 Web 中选择该账户下需要执行的供应商 1、2、3
  -> RPA 切换到供应商 1，执行任务 1-6
  -> RPA 切换到供应商 2，执行任务 1-6
  -> RPA 切换到供应商 3，执行任务 1-6
```

供应商是执行循环的第一层，任务 1-6 是每个供应商下面的任务集合。

### 25.2 供应商来源和维护方式

每个猫超大账户旗下实际能看到哪些供应商，需要登录后在页面右上角供应商下拉中确认，不能只依赖手工配置或历史分工表。

后续应增加供应商同步/查看能力：

- 登录对应猫超大账户后，读取右上角可切换的供应商列表。
- 至少保存：猫超账户、供应商 ID、供应商名称、是否当前可见、最后同步时间。
- 运营可以在 Web 中查看该账户下的供应商。
- 供应商清单可能随时间变化，不能假设永久不变。
- 供应商匹配优先使用供应商 ID，名称只作为展示和辅助匹配。

### 25.3 运营自行选择负责供应商

后期供应商负责人会变化，不能把 `6.2 阿滨组运营分工 供应链分表.txt` 永久写死为代码逻辑。

Web 中应支持运营自己维护供应商归属：

- 运营可以从自己账户可见的供应商清单中勾选负责的供应商。
- 一个供应商允许同时归属于多个运营人员。
- 品牌、类目不参与本阶段逻辑。
- 供应商归属只管理到供应商层级。
- 分工表可以作为初始导入或参考数据，但不是最终唯一来源。
- 后续供应商归属发生变化时，影响未来任务，不应改写历史任务的归属。

建议保存的业务关系：

```text
运营人员 <-> 猫超大账户 <-> 供应商
```

### 25.4 任务执行顺序

运营在网站中一次性选择需要执行的供应商后，Worker 按供应商串行执行：

```text
供应商 A：
  1. 切换右上角供应商为 A
  2. 执行任务 1
  3. 执行任务 2
  4. 执行任务 3
  5. 执行任务 4
  6. 执行任务 5
  7. 执行任务 6

供应商 B：
  1. 切换右上角供应商为 B
  2. 执行任务 1-6
```

执行要求：

- 一个供应商只执行一次。
- 同一个猫超账户下选择多个供应商时，供应商之间不能串上下文。
- 每切换一个供应商，应确认右上角当前供应商已经变更成功，再开始任务。
- 任务 1 进入实时库存页面后，只选择当前供应商并导出一张表，不再遍历实时库存页面中的供应商列表。
- 任务 2-6 也应在当前供应商上下文下执行。
- 某个供应商的某个任务无数据时，记录为业务无数据并继续下一个任务或下一个供应商。
- 某个供应商失败时，应保留供应商和任务级错误，不应把其他供应商全部标记为失败。

### 25.5 文件归属

当前阶段不处理下载表格内部内容，不按品牌或类目拆分文件。

网站整理层级应为：

```text
运营人员
  -> 供应商
    -> 任务 1-6 文件
```

同一个供应商如果被多个运营人员选择，则同一份实际文件可以同时展示/分配给这些运营人员。底层文件原则上保存一份，避免重复下载和重复存储；网站层可以有多条归属记录。

建议每次执行保存供应商级归属快照：

```text
run_id
猫超大账户
供应商 ID
供应商名称
任务编号
运营人员
raw_file
cleaned_file
执行状态
```

历史任务应使用执行当时的归属快照。后续运营修改供应商负责人时，只影响新任务，不回溯修改历史文件归属。

### 25.6 目前已知代码差距

Cursor 接手后需要先注意以下现状：

- `maochao_rpa.py` 当前任务 `realtime-inventory` 仍会发现并遍历实时库存页供应商，需要改为使用“右上角当前供应商 + 任务内同一供应商”的模式。
- 当前代码没有完整实现右上角供应商切换流程。
- 当前 Web 创建 run 时会传 `force_account_tasks: true`，可能导致不属于某账号默认任务的任务被强制执行，后续要按“供应商 -> 任务 1-6”重新设计队列输入。
- 当前账号库中的 `supplier_names` 不能继续作为长期、固定的供应商主数据。
- 当前文件归档主要按日期和账号组织，尚未形成“运营人员 -> 供应商 -> 任务文件”的网站归属层。
- 当前文件收集逻辑曾出现任务一误收相邻任务文件的情况，后续需要让下载结果绑定供应商上下文和任务上下文，不能只靠最新文件名或目录轮询判断。

### 25.7 本阶段明确不做

- 不处理实时库存表格内部字段。
- 不按品牌拆分文件。
- 不按类目拆分文件。
- 不做品牌负责人映射。
- 不把供应商负责人永久硬编码到代码。
- 不让任务 1 自动遍历当前大账户下的全部供应商。

### 25.8 建议 Cursor 的实现顺序

1. 先梳理猫超右上角供应商下拉的 DOM、点击、切换成功判定和供应商 ID 提取。
2. 在 Web 中增加供应商同步、列表展示和运营人员自主归属配置。
3. 重新设计 run 输入结构，使其明确包含猫超大账户和选中的供应商列表。
4. 将 Worker 执行单元改为“一个供应商依次执行任务 1-6”。
5. 重写任务 1：只处理当前右上角供应商，不遍历所有供应商。
6. 重梳文件收集和结果记录，绑定 `供应商 + 任务 + run_id`。
7. 增加供应商级、任务级测试，先做单账户单供应商验证，再做多供应商验证。

交接时必须明确：以上是已确认的目标业务逻辑，不代表当前代码已经实现。改代码前应先保留现有账号登录态和运行数据，优先单账户、单供应商、单任务验证。

### 25.9 任务一详细逻辑（实时库存）

任务一的执行对象是“本次 run 中选中的一个供应商”，不是“当前猫超大账户下的全部供应商”。

#### 输入

任务一必须从 run 输入中取得当前供应商：

```text
account_key
supplier_id
supplier_name
```

供应商列表由 Web 端运营提前选择并提交。任务一不得再把账号配置里的 `supplier_names`、`__first__` 或实时库存页面下拉的自动发现结果当作本次任务的供应商来源。

#### 执行步骤

对每个已选供应商，只执行一次以下流程：

```text
1. 登录猫超公司大账户并复用已有登录态
2. 打开右上角供应商切换菜单
3. 按 supplier_id 选择目标供应商
4. 验证右上角当前供应商已经变成目标供应商
5. 进入实时库存页面
6. 在实时库存页面的供应商字段中选择同一个目标供应商
7. 点击查询
8. 等待实时库存结果加载完成
9. 点击导出，并选择导出全部
10. 只等待并下载本次供应商对应的一张实时库存表
11. 记录本次供应商的任务结果
```

右上角切换和实时库存页面内的供应商选择是两个不同页面层级的动作，但目标必须是同一个 `supplier_id`。第二次选择不是重新发现或遍历供应商，只是对当前任务条件做确认。

#### 成功和无数据

- 查询到有库存数据：导出一张文件，结果为 `ok`，记录 `raw_file` 和 `cleaned_file`。
- 查询结果为 0：不点击无意义的导出或按平台实际表现处理，结果仍为 `ok`，备注明确写“该供应商无数据”，不生成文件。
- 平台导出任务明确返回无数据/null：结果为业务无数据，不应标记为系统失败。
- 一个供应商无数据不影响其他已选供应商继续执行。

#### 失败边界

- 右上角找不到目标供应商：当前供应商任务失败，记录供应商 ID、名称和原因；不要改选其他供应商代替。
- 右上角点击后无法确认已切换成功：当前供应商任务失败；不要继续进入实时库存，避免把文件归到错误供应商。
- 实时库存页面内找不到同一个供应商：当前供应商任务失败；不要使用第一个选项兜底。
- 查询成功但本次导出文件无法确认属于当前供应商：当前供应商任务失败；不要拿下载目录里最近的旧文件冒充本次结果。
- 当前供应商任务失败后，交由上层决定是否继续该供应商的任务 2-6；至少不能把后续文件标记成任务一的成功结果。

#### 文件收集约束

任务一的下载必须和以下上下文绑定：

```text
run_id
account_key
supplier_id
supplier_name
task_key = realtime-inventory
```

下载前应记录本次导出动作的起始时间或唯一文件任务标识，下载后只接受本次动作产生的文件。不能只根据“下载目录最新文件”、模糊文件名或上一个任务的文件任务按钮收集。

文件命名和目录可以继续沿用现有 raw/cleaned 结构，但结果记录中必须能反查到供应商。网站后续按供应商和运营人员归属展示，不处理文件内部品牌、类目或字段。

### 25.10 供应商归属详细逻辑

供应商归属要拆成三个概念，不能混在账号配置、任务选择和文件结果里：

```text
1. 猫超大账户能看到哪些供应商
2. 哪些运营人员负责哪些供应商
3. 某次任务产生的文件当时应该分配给谁
```

#### 供应商主数据

供应商属于猫超公司大账户。供应商的唯一身份建议使用：

```text
account_key + supplier_id
```

其中 `supplier_id` 是猫超右上角供应商下拉里的供应商 ID，`supplier_name` 只作为名称展示和辅助核对。

供应商同步规则：

- 登录某个猫超大账户后，从右上角供应商下拉获取当前可见供应商。
- 第一次发现供应商时新增主数据。
- 已存在的供应商按 `account_key + supplier_id` 更新名称、可见状态和最后同步时间。
- 供应商名称发生变化时更新显示名称，但不能因为名称变化创建新的供应商。
- 猫超页面暂时看不到某个供应商时，标记为“当前不可见/已失效”，不要物理删除。
- 供应商重新出现时恢复为可见。
- 历史任务、历史文件引用的供应商主数据不能删除。

供应商主数据的来源是猫超页面实际同步结果。`6.2 阿滨组运营分工 供应链分表.txt` 只作为初始参考或导入依据，不能作为长期固定配置，也不能阻止运营后续调整归属。

#### 运营人员归属关系

运营人员和供应商是多对多关系：

```text
一个运营人员可以负责多个供应商
一个供应商可以同时分配给多个运营人员
```

归属关系至少应包含：

```text
operator_id
account_key
supplier_id
active
created_at
updated_at
```

建议使用 `operator_id + account_key + supplier_id` 做唯一约束，避免同一人员重复绑定同一供应商。

归属维护规则：

- 运营人员可以从自己有权限的猫超大账户供应商清单中勾选、取消勾选供应商。
- 勾选表示新增或恢复自己的负责关系。
- 取消勾选表示结束自己的负责关系，不删除历史记录。
- 运营人员只能修改自己的归属；管理员可以维护全部人员的归属。
- 只能选择已经同步到该猫超大账户下的供应商，不能手工绑定一个页面从未发现的供应商。
- 供应商当前不可见时，不能作为新的执行对象，但已有归属关系和历史记录保留。
- 归属变化不影响其他运营人员对同一供应商的归属。
- 品牌、类目、供应商公司名称中的文字差异不参与归属判断，归属只到供应商 ID。

#### Web 端的供应商选择

Web 端任务创建时，运营人员看到的是“自己当前负责的、且猫超当前可见的供应商”：

```text
我的负责供应商
  -> 勾选供应商 A、B、C
  -> 创建任务
```

页面应同时显示供应商 ID 和供应商名称，避免同名供应商混淆。

创建任务时：

- 按 `account_key + supplier_id` 去重。
- 同一个供应商在一次 run 中只执行一遍任务 1-6。
- 选择多个供应商时，保留用户选择顺序或按固定供应商顺序执行，并在 run 详情中展示实际顺序。
- 创建任务前校验供应商仍然属于该猫超账户且当前可见。
- 如果供应商在提交后变成不可见，执行时应记录该供应商失败或跳过原因，不得把其他供应商的文件归到它名下。

#### 多人共有供应商

同一个供应商被多个运营人员负责时：

```text
供应商 A
  -> 运营人员甲
  -> 运营人员乙
```

执行规则：

- RPA 只为供应商 A 执行一次任务 1-6。
- 实际文件只下载和保存一份。
- 网站展示层将同一份文件关联到甲和乙名下。
- 不因为多人共有而重复切换供应商、重复执行任务或重复下载文件。

#### 任务归属快照

归属关系会变化，因此不能让历史文件实时读取当前归属。每次创建 run 时，应把当时的供应商负责人复制成归属快照：

```text
run_id
account_key
supplier_id
supplier_name_snapshot
operator_id
operator_name_snapshot
```

快照规则：

- 对本次选中的每个供应商，记录创建任务时所有有效负责人。
- 一个供应商有多个负责人，就写入多条快照。
- 任务执行完成后，任务 1-6 的文件都使用同一批供应商负责人快照。
- 任务执行过程中运营修改归属，不影响已经创建的 run。
- 任务完成后再修改归属，不回溯修改历史 run、历史文件和历史下载记录。
- 没有负责人的供应商仍然可以保留执行结果，但文件应进入“未分配/待管理员处理”，不能丢失。

#### 人员变更

运营人员离职、转组或不再负责某供应商时：

- 停用或结束该人员与供应商的当前归属关系。
- 不删除该人员的历史归属和历史任务快照。
- 历史文件仍显示在原负责人名下。
- 新任务不再把该供应商分配给该人员。
- 如果供应商同时还有其他负责人，其他人的归属不受影响。

#### 与现有分工表的关系

当前 `6.2 阿滨组运营分工 供应链分表.txt` 中存在同一供应商被多人负责的情况。该文件可用于：

- 首次初始化供应商负责人关系。
- 校验当前运营录入是否遗漏明显的初始归属。
- 作为业务参考和历史依据。

但该文件不能成为运行时唯一依据。上线自助维护后，Web 中保存的有效归属关系才是新任务的实际依据；分工表与 Web 配置不一致时，应保留差异提示，由运营或管理员确认。
